--- a/src/extension/runtime.ts
+++ b/src/extension/runtime.ts
@@ -47,7 +47,8 @@
 }
 
 function prewarmReasoningOption(level: ReturnType<ExtensionAPI["getThinkingLevel"]>): Pick<OpenAICodexStreamOptions, "reasoning"> | Record<never, never> {
-	return level === "off" ? {} : { reasoning: level };
+	if (level === "off") return {};
+	return { reasoning: level === "ultracode" ? "xhigh" : level };
 }
 
 export function createCodexExtensionRuntime(pi: ExtensionAPI): CodexExtensionRuntime {
--- a/dist/extension/runtime.js
+++ b/dist/extension/runtime.js
@@ -21,7 +21,9 @@
     return getActiveToolsInActiveOrder(pi, true);
 }
 function prewarmReasoningOption(level) {
-    return level === "off" ? {} : { reasoning: level };
+    if (level === "off")
+        return {};
+    return { reasoning: level === "ultracode" ? "xhigh" : level };
 }
 export function createCodexExtensionRuntime(pi) {
     const state = {
